## 1. Change Setup and Dependencies

- [x] 1.1 Create a short-lived branch from `main` for `2026-08-25-add-optional-oidc-auth`, verify `git branch --show-current`, and do not merge or push to `main` without human approval.
- [ ] 1.2 Add exactly `openidconnect = { version = "4.0.1", default-features = false, features = ["reqwest", "rustls-tls", "timing-resistant-secret-traits"] }` and `tower-sessions = { version = "0.15", default-features = false, features = ["axum-core", "memory-store", "private"] }`, add only maintained test-only JWT/mock support needed by the local provider, and verify `cargo check` resolves without duplicate TLS stacks or default-feature drift.
- [ ] 1.3 Add the auth module boundaries, fallible auth configuration types, injectable clock/HTTP seams, and optional shared auth state without changing auth-disabled routing; verify existing `tests/common::test_app()` builds and an auth-disabled smoke test passes.

## 2. Strict Authentication Configuration

- [ ] 2.1 Implement strict `FLOWL_AUTH_ENABLED` parsing plus disabled-by-default behavior and unit tests for absent, `false`, `true`, and malformed values; verify `cargo test config` passes.
- [ ] 2.2 Implement required enabled-mode parsing for `FLOWL_EXTERNAL_URL`, `FLOWL_OIDC_ISSUER`, client ID, client secret, and provider-name default/bounds; add tests for every missing, empty, malformed, HTTP, credential-bearing, query/fragment, and external-path case and verify enabled invalid configuration fails closed.
- [ ] 2.3 Preserve the exact configured issuer string separately, construct `IssuerUrl` directly from it, and defense-in-depth compare discovery and signature-verified token issuer text to that raw string; add tests proving `https://issuer.example` remains exact and both advertised and validly signed token `https://issuer.example/` mismatches are rejected.
- [ ] 2.4 Construct the callback solely from the validated `FLOWL_EXTERNAL_URL` and add tests showing `Host`, `Forwarded`, and `X-Forwarded-*` headers cannot alter it; verify the callback is exactly `<external-origin>/auth/callback`.

## 3. Local Mock OIDC Provider and Startup Discovery

- [ ] 3.1 Build a local generic OIDC test provider with configurable discovery metadata, authorization/token endpoints, token-auth assertions, signed ID-token claims, JWKS generations/failures, and request counters; verify a fixture self-test completes discovery and serves a verifiable token without external network access.
- [ ] 3.2 Implement startup discovery with a redirect-disabled client and cached metadata/initial JWKS; test successful discovery, unreachable/malformed discovery, unavailable/malformed initial JWKS, missing or non-HTTPS authorization/token/JWKS endpoints, missing Authorization Code support, empty/disallowed ID-token signing algorithms, discovery redirect refusal, and exact issuer mismatch, verifying enabled startup fails before serving on every invalid case.
- [ ] 3.3 Select token endpoint authentication from metadata and test `client_secret_basic`, Basic preference when both methods are offered, `client_secret_post` when Basic is absent, omitted metadata defaulting to Basic, and rejection when neither method is supported; verify the mock provider observes credentials only in the expected location.
- [ ] 3.4 Implement redacted startup/provider diagnostics and tests that preserve safe transport/status/parsing/issuer categories while excluding response bodies, client secret, request bodies, and credentials; verify captured logs contain no seeded secret marker.

## 4. Login Transactions and Safe Return Targets

- [ ] 4.1 Implement the backend bounded safe-local `return_to` type with a 2048-byte limit and exhaustive table tests for valid path/query, external URLs, protocol-relative URLs, backslashes, control characters, malformed escapes, encoded separators/normalization confusion, `/login`, `/auth/*`, and fallback `/`.
- [ ] 4.2 Implement the process-local ten-minute bounded login-transaction store with atomic consume, lazy/periodic cleanup, and private pre-auth session binding; test expiry, wrong-browser binding, concurrent consume, and callback replay without sleeping by using the injected clock.
- [ ] 4.3 Implement `GET /auth/login` with random state/nonce and PKCE S256 generated by `openidconnect`; use mock-provider assertions to verify `response_type=code`, exact redirect URI, state, nonce, challenge, and `code_challenge_method=S256`, while confirming no transaction secret reaches logs or frontend-readable storage.
- [ ] 4.4 Ensure malformed/unsafe `return_to` is replaced with `/` before transaction storage and valid backend path+query survives authorization/callback; verify focused redirect tests, including no fragment from backend-originated navigation.

## 5. Callback, Token Validation, and Sessions

- [ ] 5.1 Implement callback query/error parsing and atomic transaction consumption before token exchange; test missing/mismatched state, wrong pre-auth cookie, expired state, provider error, missing code, duplicate code/state, mixed code+error, and replay all fail without an inappropriate or second token request.
- [ ] 5.2 Exchange the code with its stored PKCE verifier, require an ID token, and validate signature/algorithm, exact raw issuer, audience, issued-at, expiry, nonce, and applicable authorized-party semantics through `openidconnect` plus strict claim policy; test valid tokens plus missing ID token, wrong verifier, signature, algorithm, validly signed slash-mismatched issuer, missing/multiple-untrusted audience, invalid `azp`, missing issued-at, expiry, missing nonce, and mismatched nonce.
- [ ] 5.3 Verify a supplied applicable `at_hash` using `openidconnect` helpers and the returned access token; test valid, mismatched, and unsupported/invalid applicable hash cases, confirming failure creates no authenticated session.
- [ ] 5.4 Configure `tower-sessions::MemoryStore` and private host-only cookie attributes (`HttpOnly`, `Secure`, `SameSite=Lax`, Path `/`); test response cookie attributes and prove no token, claim, state, nonce, verifier, code, secret, or readable session value is present.
- [ ] 5.5 Rotate/cycle the session ID only after successful validation, retain only authentication/created/absolute-expiry state, and discard provider data; test the pre-auth ID cannot authenticate, the post-auth ID differs, invalid callbacks do not rotate into auth, and no identity/token remains in session storage.
- [ ] 5.6 Enforce non-sliding absolute twelve-hour expiry with the injected clock and server-side deletion; test access immediately before/after expiry, activity not extending expiry, cookie clearing, and process-local restart invalidation.
- [ ] 5.7 Implement generic callback failure redirects and verify validation failures produce only `/login` authentication-failed state, provider transport/JWKS availability failures produce only provider-unavailable state, successful callback returns HTTP 303 to validated `return_to`, and no WebKit/OAuth completion HTML page is added.

## 6. JWKS Rotation and Recovery

- [ ] 6.1 Implement generation-based cached JWKS verification and classify only key-selection/signature failures as refresh candidates; test issuer, audience, expiry, nonce, algorithm, and `at_hash` failures do not trigger JWKS network requests.
- [ ] 6.2 Add the async deduplicated refresh gate, last-known-good retention, one verification retry, and request counters; test a newly rotated signing key succeeds after exactly one refresh and concurrent callbacks cause one network refresh.
- [ ] 6.3 Add the thirty-second failed-refresh cooldown with injectable time; test failure retains old keys, requests during cooldown do not refetch, and a request after cooldown successfully refreshes/authenticates without restarting Flowl.

## 7. Backend Route and Error Boundary

- [ ] 7.1 Add `ApiError::Unauthorized` and the fixed `AUTHENTICATION_REQUIRED` 401 JSON contract with tests for exact status/code/message and confirm all existing error-code tests still pass.
- [ ] 7.2 Compose auth-disabled and auth-enabled routers so `/health`, `/login`, `/auth/config`, login flow routes, POST logout, service worker, manifest, icons/favicon, offline page, and required immutable assets are public while `/api/info`, all `/api/*`, all `/uploads/*`, root/index/normal/unknown SPA documents are protected; verify an explicit route-policy integration matrix.
- [ ] 7.3 Test unauthenticated `/api/*` always returns JSON `401 AUTHENTICATION_REQUIRED` with no HTML/provider redirect, while unauthenticated normal online browser navigation redirects to `/login?return_to=<safe-target>` and auth-disabled behavior remains unchanged.
- [ ] 7.4 Protect `ServeDir` for originals and thumbnails and add integration tests proving unauthenticated `/uploads/*` returns 401 without bytes or redirects, authenticated requests serve files, and auth-disabled requests retain existing public serving.
- [ ] 7.5 Implement public no-store `GET /auth/config` returning only `enabled` and `provider_name`; test enabled/disabled payloads, provider-name escaping as data, omitted sensitive fields, and `Cache-Control: no-store` on every `/auth/*` and `AUTHENTICATION_REQUIRED` response.
- [ ] 7.6 Implement POST-only idempotent local `/auth/logout`, session invalidation, matching cookie expiry, and HTTP 303 to `/login?logged_out=1`; test authenticated, expired, missing-session, repeated, and GET-not-logout cases.
- [ ] 7.7 Add log-capture tests covering login, callback, token exchange, logout, and errors; verify client secrets, callback query/code/state, nonce, PKCE, tokens, cookie/session values, and provider response bodies never appear while safe operation/error categories remain useful.

## 8. Frontend Login, Shell, and Translations

- [ ] 8.1 Add typed `/auth/config` access and the frontend safe-local target utility with backend-equivalent test vectors, including preservation of SPA path/query/hash and rejection/fallback of external, malformed, auth-recursive, control-character, backslash, encoded-confusion, and oversized targets.
- [ ] 8.2 Add the public `/login` Svelte page using the existing Logo and visual tokens, exactly one `Continue with {provider}` action, no credential fields, and translated default/auth-failed/provider-unavailable/logged-out states; verify route/component tests for all states and auth-disabled navigation to `/`.
- [ ] 8.3 Update the root layout to render `/login` without sidebar/bottom nav, protected settings bootstrap, protected stores, network monitor, update notifications, or pull-to-refresh setup; verify layout tests assert no protected fetch and no shell controls on login.
- [ ] 8.4 Add matching English, German, and Spanish authentication keys, `errorCode.AUTHENTICATION_REQUIRED`, and provider interpolation; verify TypeScript dictionary-shape/error-catalog checks and login/settings tests in every locale.
- [ ] 8.5 Keep auth-related Svelte state flow one-directional and leave `ui/src/lib/pull-to-refresh.ts`/its allowlist behavior unchanged; add a regression test that route transitions do not raise `effect_update_depth_exceeded` and run the existing pull-to-refresh suite unchanged.

## 9. Centralized Frontend Session Expiry

- [ ] 9.1 Refactor shared frontend response classification so only HTTP 401 plus JSON `AUTHENTICATION_REQUIRED` assigns `/login?return_to=<current-path-query-hash>`; test arbitrary 401, other API status, invalid JSON, network rejection, and repeated/login-route handling do not trigger auth navigation.
- [ ] 9.2 Route ordinary JSON/multipart requests through the classifier and verify store/API tests preserve existing `ApiError`, localization, and `recheckHealth` behavior for non-auth and network failures.
- [ ] 9.3 Route export, AI identify, AI chat startup, summarize, import/upload, and protected-photo blob fetches through the same classifier; add focused tests proving each direct fetch path redirects on exact authentication expiry before consuming a blob/stream/body and does not redirect on network failure.
- [ ] 9.4 Update plant detail/edit route loaders to use the classifier with SvelteKit's injected fetch; test exact auth expiry navigation remains distinct from 404 and generic load errors.

## 10. Service Worker and Offline Privacy

- [ ] 10.1 Refactor testable service-worker policy so `/auth/*`, login results, redirects, callback/logout responses, and every non-200 protected response are never cached; verify unit/service-worker tests specifically cover `401 AUTHENTICATION_REQUIRED` not entering or being hidden by API/photo/application caches.
- [ ] 10.2 Cache allowlisted API and thumbnail responses only on HTTP 200, make thumbnails network-first, return received unsuccessful responses directly even when stale data exists, and use stale API/photo fallback only on rejected network fetches; test cached thumbnail + online 401, cached thumbnail + transport rejection, normal authenticated online requests, arbitrary errors, auth expiry, and temporary network loss separately.
- [ ] 10.3 Make normal application navigation network-first before every static/precache lookup so online unauthenticated navigation reaches the backend, then fall back on transport rejection to the cached app shell or branded offline page; test a precached `/index.html` cannot bypass an online 401/redirect, public login assets remain available, and existing auth-disabled offline dashboard/detail/photo behavior stays functional.
- [ ] 10.4 Add an acknowledged `PURGE_PROTECTED_CACHES` protocol plus no-controller fallback that removes current/old protected API, thumbnail, and runtime application caches while retaining login assets, manifest/icons, SW version metadata, theme, and locale; verify explicit purge contents with mocked Cache Storage.
- [ ] 10.5 Ensure neither an ordinary network outage nor a received session-expiry 401 automatically purges protected offline content; add regression tests showing cached application/API/photo data remains useful after temporary loss and remains stored after expiry until explicit logout, and that an unauthenticated installed worker can still install/update public login/offline resources.

## 11. Settings Logout Integration

- [ ] 11.1 Add the translated Settings Authentication section only when `/auth/config` reports enabled, containing Sign out and no identity/account fields; verify enabled, disabled, locale, and offline rendering tests.
- [ ] 11.2 Implement Sign out as protected-cache purge with completion acknowledgement followed by a same-origin browser POST to `/auth/logout`; verify UI tests cover purge-before-POST ordering, backend 303 navigation to logged-out state, idempotency, and retained public preferences/resources.

## 12. End-to-End Coverage, Documentation, and Review Gate

- [ ] 12.1 Add full local-provider integration flows covering Basic, Post, omitted auth metadata defaulting to Basic, PKCE/state/nonce, callback replay, invalid token claims, exact issuer slash rejection, session rotation/expiry, logout, safe `return_to`, route/API/upload policy, JWKS rotation, and failed-refresh recovery; verify the focused auth integration target passes without external services.
- [ ] 12.2 Run existing backend and frontend offline suites with auth disabled and add compatibility assertions for existing public API/SPA/upload behavior; verify `cargo test` demonstrates auth disabled and established offline behavior remain functional.
- [ ] 12.3 Update `README.md` and deployment examples with optional variables, default provider name, exact issuer warning, exact callback URI, HTTPS/reverse-proxy setup, process-local restart/multi-instance limitation, route behavior, and offline/logout privacy semantics; verify the obsolete “no built-in authentication” statement is removed without exposing implementation secrets.
- [ ] 12.4 Run `cargo fmt -- --check`, `cargo clippy -- -D warnings`, `npm run format:check --prefix ui`, `npm run lint --prefix ui`, `npm run check --prefix ui`, and `cargo test`; fix all failures and record the passing gate before requesting human review.
- [ ] 12.5 Re-run `openspec validate 2026-08-25-add-optional-oidc-auth --strict`, compare implementation against every delta scenario, and leave all changes pending human review without merging or pushing to `main`.
